export default function AppFooter() {
  return (
    <footer style={{
      borderTop: '1px solid var(--border)',
      padding: '24px 0',
      textAlign: 'center',
      color: 'var(--smoke)',
      fontSize: '0.83rem',
      marginTop: '40px',
    }}>
      🏕️ CampRate — Find your wild place
    </footer>
  )
}
